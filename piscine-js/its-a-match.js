const normal = /hi/;        // matches 'hi' anywhere
const begin = /^hi/;        // matches 'hi' only at the beginning
const end = /hi$/;          // matches 'hi' only at the end
const beginEnd = /^hi$/;    // matches exactly 'hi' (and only 'hi')
